---
tags:
  - in-progress
---

S2. 
Next: print 'Initialising BFS tree generator...', start the timer and set counters to zero. 

Find in the DB the user 'Yaroslav' or create that user if missing. 

Find in the DB the Yaroslav’s repertoire titled “Black Universal Repertoire”; if missing, create it with colour black.

Wipe all the previously generated tree ==nodes[^3]== for that repertoire. 

Does the starting position (root) exist in the position cache (DB)?

| yes | Reuse the existing one.                        |
| --- | ---------------------------------------------- |
| no  | Create it and set its probability[^2] to 100%. |

Put the root into the queue. 

Step 1 completed. 






[^1]: We need this a safeguard to protect our database from two or more scripts accidently writing simultaniously. 

[^2]: We need to be careful here. We have three types of probabilities: probability of move/position, probability of line, cumulative probability across all the lines (transpositions). * There's also probability of achieving repetition or related to repetition of position, but it is deliberately omitted since it doesn't affect logic.  

[^3]: I need a definition for the project glossary. 
