---
aliases:
  - Re-run crashes
---
**Re-run crash** — what happens when the generator is run a second time over a tree that already has content: the node search compares a full position code against stored shortened ones, never matches, and the attempt to create a duplicate node stops the run. See A1's and A4's notes. 